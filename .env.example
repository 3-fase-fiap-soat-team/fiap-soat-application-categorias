# ===========================================
# FIAP SOAT Application - Environment Variables
# ===========================================

# Application Environment
NODE_ENV=development

# ===========================================
# LOCAL DEVELOPMENT (Docker Compose)
# ===========================================
# Use these for local development with PostgreSQL container

# DATABASE_HOST=localhost
# DATABASE_PORT=5432
# DATABASE_USERNAME=postgres
# DATABASE_PASSWORD=postgres
# DATABASE_NAME=postgres
# DATABASE_SSL=false

# ===========================================
# AWS RDS INTEGRATION (Cloud Environment)
# ===========================================
# Use these when connecting to AWS RDS PostgreSQL
# Based on fiap-soat-database-terraform repository configuration

DATABASE_HOST=your-rds-endpoint.region.rds.amazonaws.com
DATABASE_PORT=5432
DATABASE_USERNAME=postgresadmin
DATABASE_PASSWORD=SuperSecret123!
DATABASE_NAME=fiap-soat-db-categorias
DATABASE_SSL=true

# ===========================================
# AWS CONFIGURATION (Optional - for Parameter Store)
# ===========================================
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key
AWS_SESSION_TOKEN=your-session-token

# ===========================================
# APPLICATION CONFIGURATION
# ===========================================
PORT=3000

# ===========================================
# MERCADO PAGO CONFIGURATION
# ===========================================
MERCADO_PAGO_ACCESS_TOKEN=your-mp-token
MERCADO_PAGO_WEBHOOK_SECRET=your-webhook-secret

# ===========================================
# NOTES:
# ===========================================
# 1. Copy this file to .env and update with real values
# 2. Never commit .env file with real credentials
# 3. For RDS connection, get the endpoint from Terraform outputs
# 4. SSL is required for RDS connections in production